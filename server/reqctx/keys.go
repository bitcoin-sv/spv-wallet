package reqctx

const (
	// AppConfigKey is the key to store the app config in the request context
	AppConfigKey = "appconfig"

	// AppEngineKey is the key to store the app engine in the request context
	AppEngineKey = "appengine"

	// AppLoggerKey is the key to store the app logger in the request context
	AppLoggerKey = "applogger"

	// UserContextKey is the key to store the user context in the request context
	UserContextKey = "usercontext"
)
